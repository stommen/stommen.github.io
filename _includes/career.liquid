<div class="col">
  <a href="{% if career.redirect %}{{ career.redirect }}{% else %}{{ career.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if career.img %}
        {%
          include figure.liquid
          loading="eager"
          path=career.img
          sizes = "250px"
          alt="career thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ career.title }}</h2>
        <p class="card-text">{{ career.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if career.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ career.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if career.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ career.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>